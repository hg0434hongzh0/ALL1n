# Changelog

## 3.5.0 - 2026-07-11

### Added

- 时间型 SQL 注入等延时场景的 `duration:` / `time:` / `elapsed:` 实际耗时判定
- `timeout:true` 预期超时条件，可将达到客户端截止时间且无 HTTP 响应的行为作为正式命中证据
- 毫秒、秒、分钟耗时单位及 `>`、`>=`、`<`、`<=`、`=`、`==` 比较运算
- 延时、预期超时、状态码、响应头、响应体和正则条件的 AND / OR 混合判定
- 可复制的 POC 验证证明摘要

### Changed

- 命中结果卡升级为正式验证结论卡，突出验证状态、POC、目标、端点、判定依据、HTTP 状态、耗时、响应大小和完整时间
- 验证证据窗口升级为适合截图复核的“POC 验证凭证”，增加正式结论横幅、指标卡、匹配证据和产品身份信息
- 证据入口更名为“查看 / 截图凭证”
- 程序副标题更新为 `By 基调听云-hongzh0`

### Safety

- 只有 `context deadline exceeded` 才能满足 `timeout:true`；手动取消和普通网络错误不会被误判为漏洞
- 超时后涉及状态码、响应头或响应体的条件会明确显示“未获得 HTTP 响应”，不会凭空判定成功
- 无效耗时单位、非正数阈值及非法 timeout 值会在发送请求前拒绝

## 3.4.0 - 2026-07-11

### Added

- 匹配规则支持响应码、响应头、响应体与正则条件的 AND / OR 布尔组合
- 支持 `&&`、`AND`、`and`、`和` 以及 `||`、`OR`、`or`、`或` 多种可读运算符
- AND 高于 OR 的明确运算优先级，兼容旧版 `&&` 规则和紧凑写法
- 每个条件输出 `✓ / ✗` 证据，OR 表达式展示全部分支及实际命中分支
- 布尔表达式语法、优先级、跨响应来源匹配及错误规则自动化测试

### Changed

- 匹配规则输入区增加逻辑运算提示和可直接套用的组合示例
- 响应体与正则匹配证据采用更清晰、统一的产品术语

### Safety

- 空分支、悬空运算符和连续运算符会在发送请求前被拒绝
- 匹配规则增强不改变授权确认、TLS 默认校验、超时与并发限制

## 3.3.0 - 2026-07-11

### Added

- 产品/文件夹节点范围识别，选中节点后递归统计并运行其全部子目录 POC
- 单目标 × 多 POC 的明确操作路径和范围预览，运行按钮实时显示节点名称与 POC 数量
- 卡片式结构化结果流，以状态色、图标和信息层级展示目标、POC、URL、状态码、耗时、响应大小与结论
- 结果状态筛选、全文搜索、显示数量统计和空结果引导
- 产品节点递归收集及单目标任务矩阵自动化测试

### Changed

- 运行验证后自动切换到“验证结果 → 结构化结果”，直接展示产品化结果卡片
- 摘要指标强化标题、辅助说明、字号和命中/错误状态色
- 当前 POC 与节点范围运行按钮分工更清晰；文件夹范围运行按钮采用高优先级样式

### Safety

- 选择产品节点本身不会自动发送请求，仍需用户确认授权并主动点击运行按钮
- 节点范围执行继续复用全局并发、超时、TLS 校验和停止控制

## 3.2.0 - 2026-07-11

### Added

- 独立验证证据中心，提供概览、原始请求、原始响应和匹配证据四个视图
- HTTP 请求行、请求头、请求体、响应状态行、响应头和响应体的结构化证据捕获
- 认证信息、Cookie、Token、密码、API Key、Session ID、私钥字段和敏感查询参数自动脱敏
- 单侧 256 KiB 证据预览限制、请求/响应截断状态与一键复制
- 完全离线、自包含、可折叠证据的 HTML 验证报告
- Runner 与 JSON / HTML 报告的证据脱敏集成测试

### Changed

- 运行结果在进入界面和报告前执行统一防御性脱敏，避免敏感 URL 查询参数出现在表格或导出文件
- CSV 导出同样复用统一结果脱敏流程，同时保留公式注入防护
- User-Agent 版本自动跟随应用版本

### Security

- HTML 报告使用模板转义处理所有不可信内容，不加载任何外部资源
- 请求与响应证据在捕获阶段脱敏，并在导出阶段进行第二次脱敏

## 3.1.0 - 2026-07-11

### Added

- 深海蓝、极光青、石墨紫、云雾白四套可即时切换并持久化的产品主题
- 现代等线、清晰黑体、系统默认三种字体策略，Windows 字体缺失时安全回退
- 多行目标资产输入、文件导入、注释、规范化、校验与去重
- `目标 × POC` 任务矩阵执行，结构化结果与 CSV 报告增加目标字段
- 原始 HTTP 请求包粘贴导入，自动映射方法、路径、参数、Headers、Body 与 Body 类型

### Changed

- 请求定义与验证结果改为互斥工作区标签页，从布局层消除区域挤压和重叠
- 关键工作区改用随主题更新的完全不透明产品面板
- 请求定义支持独立滚动，执行验证时自动切换到结果工作区
- 品牌顶栏加入主题、字体和版本控制区

### Fixed

- 请求定义与验证结果在可用高度不足时发生重叠
- TTC 字体集合无法被 Fyne 正确解析时导致的启动崩溃，改用经过启动验证的 TTF 字体

## 3.0.0 - 2026-07-11

### Added

- 可取消的当前 POC 验证和所选范围批量验证任务
- 独立的任务矩阵调度器、目标解析测试与并发取消测试
- 会话级结构化结果表格
- CSV 和 JSON 验证报告导出
- CSV 公式注入防护和 Excel 中文兼容
- 响应体大小限制、截断标记和报告字段
- POC 方法、Header、匹配规则执行前校验
- HTTP `HEAD` 和 `OPTIONS` 方法支持
- 授权目标确认机制
- 超时、并发数和 TLS 设置持久化
- Windows 发布包和 SHA256 校验文件生成脚本

### Changed

- 默认启用 TLS 证书校验
- HTTP 连接池适配批量验证
- 应用标题和 User-Agent 增加产品版本信息
- 运行统计增加取消数量

### Fixed

- Go VCS 状态异常导致项目无法构建
- Windows 下重复保存数据失败
- 数据文件依赖启动工作目录
- 中断保存后备份数据无法自动恢复
- JSON 导入接受尾随对象的问题
